# TASK-0028: critic-carryover per-finding addressed|survives|obsolete recheck classifier + ctx.critic_recheck event recorder (engine/ctx-critic-recheck-classify.sh)

Status: integrated
Area: plancritic
Target branch: `agent/integration`
Worker branch: `agent/plancritic/TASK-0028-critic-recheck-classify`
Test policy: `strict_test_first`
Gate command: `bash tests/test-ctx-critic-recheck-classify.sh && bash tests/test-engine-clean.sh`
Dispatch mode: canonical
Depends on: []

## Objective

Advance the executable DAG node `critic-carryover` (stage `S3-plan-revision`, area `plancritic`, layer `engine_runtime`, kind `runtime`) with its next smallest strict-test-first brick: the pure per-finding recheck **classifier** that maps each prior plan-critique finding to `addressed | survives | obsolete`, plus the record-only `ctx.critic_recheck` event emitter. TASK-0027 integrated the deterministic post-acceptance sampling gate (`engine/ctx-critic-recheck.sh`, behind default-OFF `GLUERUN_CRITIC_RECHECK_PCT`) that decides WHETHER an accepted task is sampled; this brick supplies the node's signature OUTPUT — the per-finding disposition set and the `ctx.critic_recheck` events named verbatim in the node's `requiredCompletion` — as a pure, side-effect-free primitive the later read-only critic-resume runner will consult.

Add a new `engine/ctx-critic-recheck-classify.sh`, auto-sourced by the existing ctx-loader glob in `lib.sh` (the `engine/ctx-*.sh` convention — this task does NOT own or edit `lib.sh`). It defines NEW functions only; NO existing engine path invokes them, so with the file present-but-uncalled the engine is byte-identical to prior behavior (mirroring TASK-0027 and the classifier/record pairing of `engine/ctx-plan-revise-dispositions.sh`, TASK-0021). It adds no driver-file hook, invokes no runner, resumes no session, and writes no state other than the one provenance event the recorder appends.

Public entry points:

- `gluerun_ctx_critic_recheck_classify <prior_critique_record> <recheck_output>` — PURE and READ-ONLY. Reads every `f-[0-9a-f]{12}` finding id from the prior `gluerun.orchestration.plan-critique.v0` record and the resumed critic's recheck self-report at `<recheck_output>` (JSON in which the critic reports, per prior finding id, a status in `addressed | survives | obsolete` — e.g. a `findings` array of `{id,status}` objects). Prints one TAB-separated `<finding-id>\t<disposition>` line per prior finding in id-sorted order; `disposition ∈ {addressed, survives, obsolete}`. Conservative skeptic-safe fail-closed default = `survives` for any id whose status cannot be positively established — a missing/unparseable record or output, an unknown/ambiguous status, or an id absent from the recheck output — NEVER fabricating `addressed` or `obsolete`. Appends no event, mutates nothing, ALWAYS exits 0.
- `gluerun_ctx_critic_recheck_record <node> <run_id> <task_id> <prior_critique_record> <recheck_output>` — Records ONLY. Classifies (via the pure function above) and emits EXACTLY ONE `ctx.critic_recheck` event through `gluerun_append_event` carrying `node`, `runId`, `taskId`, `role` = `plan-critic` (skeptic), and the full id-sorted per-finding id→disposition set. It changes no outcome, promotes/quarantines no candidate, invokes no runner, touches no lease, and never mutates any accept/reject decision. Events land only in the pinned `GLUERUN_EVENTS_FILE`.

Advocate/skeptic line + evidence invariance: the recheck records only — it never blocks or changes any accept/reject outcome, never weakens a gate, and never makes the fresh implementation auditor bypassable; the conservative `survives` default keeps the critic a skeptic (an unproven concern is assumed to persist). The whole recheck stays default-OFF behind `GLUERUN_CRITIC_RECHECK_PCT` (TASK-0027), consulted upstream by the follow-up runner. The read-only critic-session RESUME execution over the accepted diff, the skeptic role gate (this session may never be resumed as planner/implementer), and the `l1-drive.sh` post-acceptance hook are the sanctioned follow-up slices of `critic-carryover` and are OUT OF SCOPE here.

## Scope

Owned files:

- `engine/ctx-critic-recheck-classify.sh`
- `tests/test-ctx-critic-recheck-classify.sh`

Forbidden files:

- `engine/lib.sh`
- `engine/ctx-critic-recheck.sh`
- `engine/l1-drive.sh`
- Any file outside the owned scope.

## Acceptance Criteria

- `engine/ctx-critic-recheck-classify.sh` is auto-sourced by the ctx-loader glob in `lib.sh` and defines NEW functions only; no existing engine path calls them, so the engine is byte-identical to prior behavior when the file is present-but-uncalled (advances node `critic-carryover`, layer `engine_runtime`).
- `gluerun_ctx_critic_recheck_classify` is PURE/READ-ONLY: it reads every `f-[0-9a-f]{12}` finding id from the prior `plan-critique.v0` record, prints `<finding-id>\t<disposition>` per prior finding in id-sorted order with `disposition ∈ {addressed, survives, obsolete}`, appends no event, and writes no state; it always exits 0.
- Conservative fail-closed classification: an id whose recheck status cannot be positively established — missing/unparseable prior record or recheck output, unknown/ambiguous status, or an id absent from the recheck output — defaults to `survives`; the classifier NEVER fabricates `addressed` or `obsolete` on ambiguity.
- `gluerun_ctx_critic_recheck_record` records ONLY: it emits exactly ONE `ctx.critic_recheck` event via `gluerun_append_event` carrying `node`, `runId`, `taskId`, `role` = `plan-critic`, and the full id-sorted per-finding disposition set; it changes no outcome, invokes no runner, touches no lease, and never mutates any accept/reject decision (evidence invariance).
- Feature-flag discipline: this brick adds no new default-ON behavior; the recheck remains default-OFF behind `GLUERUN_CRITIC_RECHECK_PCT` (TASK-0027) and the engine is byte-identical when the new file is present-but-uncalled.
- `tests/test-ctx-critic-recheck-classify.sh` is the strict-test-first proof (red before implementation, green after) covering: explicit `addressed`, `survives`, and `obsolete` statuses; the conservative `survives` default for an unknown/ambiguous status, for an id absent from the recheck output, and for a missing/unparseable record or output; id-sorted determinism; and exactly one `ctx.critic_recheck` event carrying the full disposition set with `role` = `plan-critic` and no other state write.
- The gate command `bash tests/test-ctx-critic-recheck-classify.sh && bash tests/test-engine-clean.sh` passes (no project-specific symbols introduced in `engine/`).
