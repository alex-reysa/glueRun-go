# TASK-0030: critic-carryover post-acceptance recheck read-only accepted-diff context assembler over prior findings (engine/ctx-critic-recheck-context.sh)

Status: accepted
Area: plancritic
Target branch: `agent/integration`
Worker branch: `agent/plancritic/TASK-0030-critic-recheck-context`
Test policy: `strict_test_first`
Gate command: `bash tests/test-ctx-critic-recheck-context.sh && bash tests/test-engine-clean.sh`
Dispatch mode: canonical
Depends on: []

## Objective

Advance the executable DAG node `critic-carryover` (stage `S3-plan-revision`, area `plancritic`, layer `engine_runtime`, kind `runtime`) with a bounded strict-test-first brick whose prerequisites are ALL integrated: the pure, read-only assembler that composes the recheck context the resumed plan-critic reads — the ACCEPTED DIFF paired with the critic's OWN PRIOR FINDINGS as the checklist, framing the skeptic recheck question `for each of your prior concerns: addressed | survives | obsolete`. It is the post-acceptance sibling of the integrated staged-candidate critic context assembler `gluerun_ctx_plan_critic_context` (TASK-0014, `engine/ctx-plan-critic-context.sh`): TASK-0014 composes the context for the PLANNING-time critique of staged candidate files; this brick composes the context for the POST-ACCEPTANCE recheck over the accepted diff. Different inputs (accepted diff + prior findings vs staged candidate files) and different purpose (addressed/survives/obsolete recheck vs initial critique), so it advances the stage rather than duplicating TASK-0014.

This brick builds ONLY on already-integrated primitives — git (for the accepted diff), the `gluerun.orchestration.plan-critique.v0` record shape (TASK-0012), the sampling knob (TASK-0027), and the classifier/recorder (TASK-0028) — and does NOT depend on the resume authority decider (`engine/ctx-critic-recheck-resume.sh`, TASK-0029), which is `accepted` but NOT yet integrated into `agent/integration`. The read-only critic-resume RUNNER that consumes this context together with the TASK-0029 resume authority and the TASK-0028 recorder, and the `l1-drive.sh` post-acceptance hook, are the sanctioned follow-up slices and are OUT OF SCOPE here.

Add a new `engine/ctx-critic-recheck-context.sh`, auto-sourced by the existing ctx-loader glob in `lib.sh` (the `engine/ctx-*.sh` convention — this task does NOT own or edit `lib.sh`). It defines NEW functions only; NO existing engine path invokes them, so with the file present-but-uncalled the engine is byte-identical to prior behavior (mirroring TASK-0014 / TASK-0027 / TASK-0028).

Public entry points:

- `gluerun_ctx_critic_recheck_accepted_diff <base_ref> <head_ref> [worktree]` — PURE and READ-ONLY. Prints the accepted diff between the pre-acceptance base and the accepted/merged head via `git diff` in `[worktree]` (default `.`). An empty/indeterminate ref, or a non-repo worktree, yields empty output (fail-safe, never a crash). It makes no commit/checkout/reset and mutates neither the worktree nor any state.
- `gluerun_ctx_critic_recheck_context <node> <task_id> <prior_critique_record> <accepted_diff_file> <out_file>` — PURE assembler. Composes into `<out_file>`: (1) a header framing the skeptic recheck question (per prior concern, answer addressed | survives | obsolete); (2) the per-finding checklist read from the prior `plan-critique.v0` record at `<prior_critique_record>`, emitting each finding id-sorted with its id, severity, claim, and evidence plus the explicit per-finding recheck instruction; (3) the accepted-diff content from `<accepted_diff_file>`. It writes ONLY `<out_file>`, appends no event, invokes no runner, and mutates nothing else. Deterministic: findings emit in id-sorted order so the composed context is byte-stable across runs for a fixed input set.

Advocate/skeptic line + evidence invariance: the assembler reads staged inputs and writes ONLY its single composed output file. It changes no accept/reject outcome, weakens no gate, never lets a resumed context satisfy an independence-required step, and never makes the fresh implementation auditor bypassable. The whole recheck stays default-OFF behind `GLUERUN_CRITIC_RECHECK_PCT` (TASK-0027), consulted upstream by the follow-up runner.

## Scope

Owned files:

- `engine/ctx-critic-recheck-context.sh`
- `tests/test-ctx-critic-recheck-context.sh`

Forbidden files:

- `engine/lib.sh`
- `engine/l1-drive.sh`
- `engine/ctx-plan-critic-context.sh`
- `engine/ctx-critic-recheck.sh`
- `engine/ctx-critic-recheck-classify.sh`
- Any file outside the owned scope.

## Acceptance Criteria

- `engine/ctx-critic-recheck-context.sh` is auto-sourced by the ctx-loader glob in `lib.sh` and defines NEW functions only; no existing engine path calls them, so the engine is byte-identical to prior behavior when the file is present-but-uncalled (advances node `critic-carryover`, layer `engine_runtime`).
- `gluerun_ctx_critic_recheck_accepted_diff` is PURE/READ-ONLY: it prints the `git diff` between the pre-acceptance base and the accepted head in the worktree; an indeterminate ref, an empty ref, or a non-repo worktree yields empty output; it makes no commit/checkout/reset and mutates neither the worktree nor any state.
- `gluerun_ctx_critic_recheck_context` composes into `<out_file>` a header framing the skeptic recheck question (per prior concern: addressed | survives | obsolete), the id-sorted per-finding checklist read from the prior `plan-critique.v0` record (id, severity, claim, evidence), and the accepted-diff content from `<accepted_diff_file>`; it writes ONLY `<out_file>`, appends no event, invokes no runner, and mutates nothing else.
- Determinism and graceful degradation: for a fixed input set the composed `<out_file>` is byte-stable across runs (findings emitted in id-sorted order); a missing/unparseable prior record or a missing/empty accepted-diff file degrades to a well-formed context with an empty checklist and/or empty diff section (never a crash) and never fabricates findings.
- Evidence invariance / advocate-skeptic line: the assembler is read-only and changes no accept/reject outcome, weakens no gate, and never makes the fresh implementation auditor bypassable; the whole recheck remains default-OFF behind `GLUERUN_CRITIC_RECHECK_PCT` (TASK-0027) and the engine is byte-identical when the new file is present-but-uncalled.
- `tests/test-ctx-critic-recheck-context.sh` is the strict-test-first proof (red before implementation, green after) covering: accepted-diff resolution over a small git fixture and empty output for indeterminate refs / a non-repo worktree; an id-sorted per-finding checklist assembled from a `plan-critique.v0` record; the accepted diff embedded in the composed context; byte-stable determinism across repeated runs; graceful empty-checklist/empty-diff behavior on missing/unparseable inputs; and that ONLY `<out_file>` is written (no events, no other state).
- The gate command `bash tests/test-ctx-critic-recheck-context.sh && bash tests/test-engine-clean.sh` passes (no project-specific symbols introduced in `engine/`).
