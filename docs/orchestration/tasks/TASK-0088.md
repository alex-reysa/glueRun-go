# TASK-0088: graph-projector cli hook — gluerun graph rebuild/sync/query behind GLUERUN_CTX_GRAPH (cli/gluerun)

Status: integrated
Area: graph
Target branch: `agent/integration`
Worker branch: `agent/graph/TASK-0088-gluerun-graph-cli`
Test policy: `strict_test_first`
Gate command: `bash tests/run.sh`
Dispatch mode: canonical
Depends on: []

## Objective

Expose the integrated `graph-projector` (layer `engine_runtime`) engine functions as the user-facing `gluerun graph rebuild/sync/query` subcommand, behind `GLUERUN_CTX_GRAPH` (default 0) — the last `requiredCompletion` piece. This is the single sanctioned `cli/gluerun` driver-hook for this node (the stage file: the cli hook chains behind metrics-extract for that file); it touches an existing driver file, so it carries the full-suite gate and lands alone. Implement one minimal call-site hook that mirrors the integrated `cmd_metrics` delegation pattern:

- Add a `cmd_graph` delegator to `cli/gluerun` that sub-dispatches `rebuild [stateDir] [graphDir]` to `gluerun_graph_rebuild`, `sync [stateDir] [graphDir]` to `gluerun_graph_sync`, and `query <neighbors|lineage|open-contradictions> [args]` to the corresponding integrated `gluerun_graph_query_*` reader, resolving sensible stateDir/graphDir defaults and adding NO projection logic of its own.
- Add one `graph` arm to the existing command-dispatch block so `gluerun graph ...` reaches `cmd_graph`.
- Gate the entire subcommand behind `GLUERUN_CTX_GRAPH`: when it is unset or `0`, `gluerun graph ...` refuses exactly as an unknown command did before this hook, and the help/usage output is unchanged (byte-identical OFF-parity). Any `graph` usage/help line appears only when the flag is enabled.

The hook only delegates into the integrated `engine/ctx-graph*.sh` functions (auto-sourced by the ctx-loader block). This task advances `graph-projector` toward its `requiredCompletion` by making rebuild/sync/query invocable behind the flag; it proposes the full `requiredCompletion` behavior (rebuild/sync/query behind GLUERUN_CTX_GRAPH, idempotent + deterministic, rebuild equals sync, loss-free) as a gate candidate but does not declare the node complete.

## Scope

Owned files:

- `cli/gluerun`
- `tests/test-gluerun-graph-cli.sh`

Forbidden files:

- Any file outside the owned scope.

## Acceptance Criteria

- `bash tests/run.sh` is green (full suite — this task owns the existing driver file `cli/gluerun`).
- `tests/test-gluerun-graph-cli.sh` is green and is the strict-test-first regression: it fails before the hook exists (RED) and passes after (GREEN).
- With `GLUERUN_CTX_GRAPH=1`: `gluerun graph rebuild [stateDir] [graphDir]` delegates to `gluerun_graph_rebuild` and writes the canonical corpus; `gluerun graph sync [stateDir] [graphDir]` delegates to `gluerun_graph_sync`; `gluerun graph query neighbors|lineage|open-contradictions [args]` delegates to the matching `gluerun_graph_query_*` function and prints its output. None of the arms add projection logic.
- OFF-parity (byte-identical): with `GLUERUN_CTX_GRAPH` unset or `0`, `cli/gluerun` behaves exactly as before the hook — `gluerun graph ...` refuses just as an unknown command did, and `gluerun --help`/usage output is unchanged.
- Bad or missing `graph` subcommand / arguments produce a clear usage error with a non-zero exit, not a crash or a stack trace.
- Minimal footprint: the change is a `cmd_graph` delegator plus one dispatch arm (and a flag-gated usage line), consistent with the driver-hook scarcity rule.

## Context packet

### Decisions
- OFF-parity is preserved by gating the whole `graph` subcommand AND its help/usage line behind `GLUERUN_CTX_GRAPH`: with the flag off, typing `gluerun graph` hits the same unknown-command refusal as before, so `cli/gluerun` stays byte-identical.
- The hook is a thin delegator mirroring `cmd_metrics`: it resolves stateDir/graphDir defaults and calls the integrated `gluerun_graph_rebuild`/`gluerun_graph_sync`/`gluerun_graph_query_*`, keeping the driver-file footprint to a minimal call site per the planner contract's driver-hook scarcity rule.

### Assumptions
- [validated] `gluerun_graph_rebuild`, `gluerun_graph_sync`, and the `gluerun_graph_query_*` readers are integrated and auto-sourced via the ctx-loader block — basis: TASK-0082/0084/0086 integrated.
- [validated] `cli/gluerun` dispatches subcommands via a cmd-dispatch case block with per-command `cmd_*` functions and a static usage list — basis: the integrated `cmd_metrics` hook and dispatch arm.
- [open] the CLI can resolve a default state dir (e.g. `.gluerun-state`) to pass as the rebuild/sync stateDir — basis: the real `.gluerun-state` tree the engine already reads.

### Rejected alternatives
- Adding a static `graph` usage line unconditionally — rejected: it would change `gluerun --help` output when the flag is off, breaking OFF byte-identity; the line shows only when `GLUERUN_CTX_GRAPH` is enabled.
- Splitting rebuild/sync/query into three separate cli hooks — rejected: they share one `cmd_graph` dispatch in the same existing driver file; three tasks would each re-own `cli/gluerun` and pay three full-suite gates for one call site.

### Inspected symbols
- `cmd_metrics` + the command-dispatch case block (`cli/gluerun`): the exact pattern the graph hook mirrors — a `cmd_graph` delegator plus one `graph` dispatch arm — which is why this is a minimal call-site hook, not new logic.
