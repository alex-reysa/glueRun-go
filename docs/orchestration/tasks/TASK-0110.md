# TASK-0110: rel-02-try-hygiene — clear stale worker output between tries and pin the try>0-fresh invariant

Status: accepted
Area: session
Target branch: `agent/integration`
Worker branch: `agent/session/TASK-0110-try-hygiene`
Test policy: `strict_test_first`
Gate command: `bash tests/test-per-try-artifacts.sh && bash tests/test-session-affinity.sh && bash tests/test-engine-clean.sh`
Dispatch mode: canonical
DAG node: rel-02-try-hygiene
Depends on: [TASK-0109]

## Objective

Two hygiene defects in the worker try loop of `engine/l1-drive.sh` `run_worker_phase`:

1. Stale `last-message.json` masks failures. Every try passes
   `--output-last-message "$run_dir/last-message.json"`, and the file is never removed between
   tries. If try 1 dies before writing output, failure classification is handed try 0's stale
   file, which can suppress an `empty-output` classification. The auditor loop already does
   this correctly — it runs `rm -f "$audit_record"` at the top of every try. Mirror that:
   remove `last-message.json` (and any per-try equivalent) at the top of each worker try
   before the runner spawns.

2. The try>0-fresh invariant is enforced by code but asserted by no test. The gate
   `[[ "$worker_try" -eq 0 && -n "$worker_resume_id" ... ]]` (and the auditor twin at
   `audit_try -eq 0`) is the only thing keeping an infra retry from resuming an advocate
   session out-of-contract. `tests/test-session-affinity.sh` asserts attempt-1-fresh and
   attempt-2-resume at the decision boundary, but nothing drives an infra retry and asserts
   the second try's argv omits `--resume-session`. Add that regression test for both roles.
   TASK-0124 will later relax this gate deliberately; this test is the contract it must
   renegotiate rather than silently break.

3. The rel-01 archive fixture's literal whole-tree equality is not concurrency-safe. During
   `RUN-20260814T010432Z-76912`, both host gates failed only because the concurrently running
   TASK-0115 appended its own legitimate run evidence beneath the shared `.singular-state`.
   Replace the recursive equality checks over the complete live checkout, shared state root,
   and authoritative event bytes with a causal leakage proof that remains strict at
   `maxConcurrent=3`:

   - capture the literal caller checkout/state/event paths before any fixture rebinding;
   - seed private fixture-local source-time decoy state, event, and config paths with canaries;
     these decoys are distinct from both active iteration event logs, and their complete
     inventories must remain byte-identical after each archive call and final drain;
   - generate one unguessable nonce, include it in each distinct fixture run ID and marker, run
     the real archive helper twice in one shell process, and require its event emission exactly
     once in each active fixture event log with no cross-iteration event;
   - require the nonce to remain absent from the literal authoritative event file and from every
     nonce-named path under the literal caller-state namespace;
   - fingerprint fixed literal caller-checkout protected files (`.gitignore`, `engine/lib.sh`,
     `engine/l1-drive.sh`, and `tests/test-per-try-artifacts.sh`) by type, mode, and content before
     and after each call and the bounded final drain; and
   - do not compare the complete live checkout, shared state tree, or authoritative event bytes:
     unrelated sibling lifecycle writes are valid and must not make the focused test red.

   Preserve the existing archive-copy assertions, two same-process executions, distinct roots,
   fixture-event isolation, and bounded late-writer drain.

Recovery seed (mandatory prompt input): no accepted commit exists for
`RUN-20260814T010432Z-76912`; attempts 1 and 2 are evidence only. Reconstruct test-first from
`.singular-state/recovery-snapshots/RUN-20260814T010432Z-76912/20260814T014300Z/original/attempts/2/{last-message.json,worker-attempt-2-try-0.log}`
and attempt 1's RED evidence. The production recipe is exactly two
`rm -f "$run_dir/last-message.json"` calls: before every worker invocation and before the
same-try rc86 fresh fallback. Rebuild the stale-output and both-role retry tests together with
the amended hermeticity harness. Do not reuse the old packet, gate result, or diff as approval.

Second recovery boundary (mandatory prompt input): `RUN-20260814T022422Z-15250` and candidate
`refs/singular/operator-candidates/TASK-0110/RUN-20260814T022422Z-15250-attempt-1` at
`fa95b8dcc7e5c7f75b8a355135ea09bdcbf49456` are evidence only. The primary auditor accepted,
but the independent paired audit returned `needs-fix`; the accepted packet is quarantined and
must never be reused as approval. A fresh reset must retain the exact two-line production recipe
while rebuilding the proof so that:

- a deterministic sibling writer publishes its PID, becomes ready before either archive call,
  acknowledges at least one write while each call is on-stack, performs a final requested write
  before drain, exits zero, and leaves an asserted exact write count;
- source-time decoys use an independent `decoy_canary` that is unequal to and contains no
  `archive_nonce`; the archive nonce remains confined to the deliberate fixture run IDs and
  markers named below;
- the rc86 resume-refusal fixture first writes a stale `last-message.json`, then the same-try
  fresh fallback writes no result and exits nonzero, proving the second cleanup by the final
  `worker-infra` classification and absent stale file; and
- the independently verified operator overlay used to dispatch this rerun passes the
  accepted-root packet manifest precondition that TASK-0113 later owns permanently: the final
  manifest's `packet.json` reference, the accepted root packet, and the inbox copy must be byte-
  and SHA-identical. This is a runtime dispatch precondition, not a DAG dependency from rel-02
  to rel-05. A mismatch is a failed acceptance transaction, never an evidence waiver.

## Scope

Owned files:

- `engine/l1-drive.sh`
- `tests/test-per-try-artifacts.sh`
- `tests/test-session-affinity.sh`

Forbidden files:

- Any file outside the owned scope. Do not modify `engine/lib.sh` (the resume decider is
  out of scope here).

## Acceptance Criteria

- RED first: a stub runner writes a valid `last-message.json` on try 0 then produces no output
  on try 1; current behavior misclassifies try 1. GREEN: try 1 classifies as `empty-output`
  (or its per-policy class), proven in `tests/test-per-try-artifacts.sh`.
- New assertions in `tests/test-session-affinity.sh`: with a resumable session recorded, force
  a worker infra retry and an auditor infra retry; the try-1 argv of each contains no
  `--resume-session`, while attempt-level resume behavior is unchanged.
- The auditor's existing per-try `rm -f` behavior is untouched.
- The archive hermeticity fixture passes while an unrelated sibling writer mutates the literal
  shared state. Its event emission is present only in the two active fixture-local event logs,
  never the literal caller event file or caller-state namespace; source-time decoy hostile
  state/event/config paths and the fixed protected caller files remain byte-identical after
  each call and final drain. The nonce may otherwise appear only where deliberately encoded in
  fixture run IDs and markers. The writer's ready/overlap/final-write handshakes, PID, exact
  write count, and zero exit status make that concurrency causal rather than scheduler-lucky;
  decoy contents use a separate canary and contain no archive nonce.
- The live rc86 refusal/fresh-fallback test is mutation-sensitive to the second cleanup: the
  refusal leaves stale output, the fresh fallback emits none and fails, the stale file is absent,
  and the attempt classifies `worker-infra` without changing attempt/try accounting.
- The accepted run's final evidence manifest resolves `packet.json` to the byte-identical,
  accepted root/inbox packet under the corrected controller runtime.
- Full gate command passes.
