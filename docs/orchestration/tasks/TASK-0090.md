# TASK-0090: graph-projector composed end-to-end requiredCompletion guard — rebuild determinism + sync≡rebuild + loss-free + query + authoritative/claim + OFF-parity (tests/test-ctx-graph.sh)

Status: integrated
Area: graph
Target branch: `agent/integration`
Worker branch: `agent/graph/TASK-0090-graph-requiredcompletion-guard`
Test policy: `strict_test_first`
Gate command: `bash tests/run.sh`
Dispatch mode: canonical
Depends on: []

## Objective

Make the canonical `tests/test-ctx-graph.sh` the composed end-to-end requiredCompletion guard for the `graph-projector` (layer `engine_runtime`) node — satisfying the requiredCompletion clause `test-ctx-graph.sh green` with a test that actually exercises `rebuild`/`sync`/`query` in one walk, not just the primitives it covers today. This is the node's capstone, mirroring the sibling nodes' closing guards (TASK-0069, TASK-0087). It owns the existing canonical test file, so it carries the full-suite gate and lands alone. Extend `tests/test-ctx-graph.sh` additively with a single requiredCompletion walk that composes the integrated engine functions (`singular_graph_rebuild`, `singular_graph_sync`, `singular_graph_query_*`, the mappers and loss-free writer) over a fixture state tree:

1. Build a fixture `<stateDir>` exercising several source families — including an `authoritative` commit and gate-result plus `claim` nodes — so the corpus spans node/edge types and both evidence classes.
2. Assert rebuild determinism (byte-identical `nodes.jsonl` + `edges.jsonl` on repeated `singular_graph_rebuild`), graph deletable + rebuildable loss-free (delete the graph dir, rebuild, byte-identical), sync equals rebuild (`singular_graph_sync` from scratch and incrementally after appending events yields a corpus byte-identical to a full rebuild over the augmented state), the `query` outputs (neighbors, lineage, open-contradictions), and authoritative/claim separation preserved (commit + gate-result `authoritative`, every other node `claim`; every line valid vs `schemas/context-graph.v0.schema.json`).
3. Assert OFF-parity end-to-end: with `SINGULAR_CTX_GRAPH` unset or `0` the engine is byte-identical (sourcing invokes nothing, writes no file) and `singular graph ...` refuses as before — pinned behaviorally by a directory snapshot, never by an absence-grep (planner-contract rule 9).

The guard only asserts requiredCompletion behaviors over the integrated functions; it re-implements no projection logic. This task advances `graph-projector` to its capstone and proposes the exit gate as a candidate; it does not declare the node or area complete.

## Scope

Owned files:

- `tests/test-ctx-graph.sh`

Forbidden files:

- Any file outside the owned scope.

## Acceptance Criteria

- `bash tests/run.sh` is green (full suite — this task owns the existing `tests/test-ctx-graph.sh`).
- `tests/test-ctx-graph.sh` retains its existing primitives coverage AND adds the end-to-end requiredCompletion walk; the new assertions are strict-test-first (they would fail were any of rebuild/sync/query/loss-free behavior absent).
- Over the fixture state tree: `singular_graph_rebuild` is byte-identical on repeated runs; deleting the graph dir and rebuilding reproduces the byte-identical corpus (loss-free); `singular_graph_sync` from scratch and after appending events is byte-identical to a full rebuild over the augmented state (sync equals rebuild).
- The `query` readers (`singular_graph_query_neighbors`, `singular_graph_query_lineage`, `singular_graph_query_open_contradictions`) return the documented outputs over the rebuilt corpus.
- Authoritative/claim separation preserved: commit and gate-result nodes are `authoritative`; every other node is `claim`; every corpus line validates against `schemas/context-graph.v0.schema.json`.
- OFF-parity: with `SINGULAR_CTX_GRAPH` unset or `0`, the engine is byte-identical (no writes, nothing invoked) and `singular graph ...` refuses as before; the guard pins this behaviorally via a snapshot, not via an absence-grep.
- Gate candidate: this guard is proposed as the evidence for the `graph-projector` exit gate (rebuild/sync/query behind `SINGULAR_CTX_GRAPH`; idempotent + deterministic; rebuild equals sync on fixtures; graph deletable + rebuildable loss-free; `test-ctx-graph.sh` green); the task does not itself declare the node complete.

## Context packet

### Decisions
- The canonical `tests/test-ctx-graph.sh` becomes the composed end-to-end guard (mirroring the sibling nodes' capstones TASK-0069 and TASK-0087) so the requiredCompletion clause `test-ctx-graph.sh green` is met by a test that exercises rebuild/sync/query in one walk, not just the primitives.
- The guard composes the integrated engine functions over a fixture state tree rather than re-implementing projection; it asserts only requiredCompletion behaviors, keeping it a pure verification capstone.

### Assumptions
- [validated] `singular_graph_rebuild` (TASK-0082), `singular_graph_sync` (TASK-0084), the `singular_graph_query_*` readers (TASK-0086), the mappers/writer, and the `singular graph` cli hook (TASK-0088) are integrated — basis: the existing-tasks list.
- [validated] `tests/test-ctx-graph.sh` today covers only the primitives, so adding the end-to-end walk is additive — basis: the file's own header and assertions.
- [open] `python3` is available for the fixture build and JSON checks in the guard — basis: prior graph tests rely on it.

### Rejected alternatives
- Shipping the guard as a new `tests/test-ctx-graph-e2e.sh` — rejected: the requiredCompletion string and the stage file name `tests/test-ctx-graph.sh` specifically as the rebuild/sync/query test, so the canonical file must carry the guard.
- Re-testing each verb only in isolation (already covered by the per-verb tests) — rejected: the capstone's value is the single requiredCompletion walk (rebuild -> sync -> query -> loss-free -> authoritative/claim -> OFF-parity) the operator reads as gate evidence.
