# TASK-0115: rel-07-run-control — authenticated read-only deadline and cancel control

Status: ready
Area: packets
Target branch: `agent/integration`
Worker branch: `agent/packets/TASK-0115-run-control-readonly`
Test policy: `strict_test_first`
Gate command: `bash tests/test-run-control.sh && bash tests/test-schema-sync.sh && bash tests/test-engine-clean.sh`
Dispatch mode: canonical
DAG node: rel-07-run-control
Depends on: [TASK-0108]

## Objective

Every provider runner and the gate freeze their timeout at spawn:
`deadline=$((SECONDS + budget))` computed once above the poll loop (`engine/codex-run.sh`,
`engine/claude-run.sh`, `engine/cursor-run.sh`, `engine/grok-run.sh`, `engine/gemini-run.sh`,
`engine/opencode-run.sh`, `engine/gate-check.sh`), with no reload path anywhere. Field
consequence (audited, reproduced from real run state): an operator raising the budget mid-run
has zero effect on the running try — three consecutive kills each fired at the exact stale
budget, every raise landing after the kill it was meant to prevent.

The first dogfood implementation proved that a plain JSON file cannot also satisfy the authored
host-authority boundary. Provider and gate children run as the same UID as the host; hiding paths,
scrubbing environment variables, chmod/ACL/chflags, or a deprecated platform sandbox cannot stop
an arbitrary child from deriving `.singular-state/runs`, replacing the file, and self-extending.
The control must therefore be writable but not *authorable* by the supervised identity: only a
run-bound asymmetric signature from an external operator authority makes a record honor-able.

Required changes:

1. New sourced helper `engine/run-control.sh` verifies an optional signed
   `<control_dir>/run-control.json`. The v1 record contains exactly `schema`, `runId`,
   monotonically increasing `generation`, `deadlineExtendSec`, `cancel`, `reason`, `updatedAt`,
   `signerKeyId`, and `signature`. Verify an Ed25519 signature over a documented,
   domain-separated canonical payload containing every field except `signature`. The expected
   run ID, signer key ID, and public-key bytes are pinned in parent memory before the child
   spawns and are never re-read from a child-writable path. The
   production private signing key must never enter the repository, worktree, state root, child
   environment, argv, or supervised UID's runtime filesystem. Operators supply a pre-signed
   record; writer/key provisioning remains outside this task. Tests may create an ephemeral
   fixture key only in parent-process memory or an anonymous pipe before the adversarial child
   exists; its private bytes must be erased before spawn and must never appear in a filesystem
   path, environment, or argument. Resolve and pin every verification dependency before child
   spawn. A verifier is trusted only when it is pinned before spawn through a root-owned,
   non-group/other-writable canonical regular executable chain, or is an equivalently immutable
   in-process verifier whose exact code is already resident and is never executed through a
   mutable path. Byte-hashing a tool and later reopening its pathname is not sufficient. A PATH
   lookup, a sandbox-relative `-w` result, a user-owned Homebrew/venv binary, or a replaceable
   symlink is not a trust boundary. If no trusted verifier exists, signed control fails closed
   while no-control behavior remains unchanged. If a verifier process is started after child
   spawn, every executable, library, and imported-code origin it can load must be resolved and
   proven root-owned and non-group/other-writable before spawn; isolation flags alone are not
   that proof. Prefer a vetted RFC 8032 implementation. Any in-tree implementation must reject
   non-canonical and weak/small-order public keys and pass published valid and invalid vectors.
2. New additive schema `schemas/run-control.v1.schema.json` with
   `schema: "singular.orchestration.run-control.v1"`, RFC3339 `updatedAt`, non-negative integer
   generation/extension, boolean cancel, bounded strings (including an empty `reason`), base64
   signature, and `additionalProperties: false`. Add the byte-identical consumer mirror
   `schemas/orchestration/run-control.v1.schema.json`; the runtime hard protocol ceiling and both
   schemas must declare the same maximum extension. The configured per-invocation cap may be
   lower; any configured value above the hard protocol ceiling is clamped down to it. Snapshot at
   most 16 KiB from a non-symlink regular file
   without blocking or following a replaced path; symlink or any non-regular file (including
   FIFO/device/socket/directory), unreadable, changed-during-snapshot, duplicate-member,
   unsigned, malformed, schema-invalid, modified, wrong-run, wrong-key, replayed/non-increasing
   within the current bounded invocation, negative, or oversized records are absent.
3. In each of the six runner poll loops and the two gate poll loops (`gate-check.sh` and the
   duplicated loop in `audit-verify.sh`): once per iteration (the loops already wake at
   0.1-2 s), at that loop's existing legacy deadline decision point—after any pre-existing
   completion/grace/idle processing—first terminate an already-expired effective deadline,
   then consult the helper. Do not reorder any legacy completion, grace, or idle logic. Within
   each bounded poll-loop invocation, accept only increasing
   generations, retain monotonic
   `max(deadlineExtendSec)`, make cancellation sticky, and apply
   `effective_deadline = spawn_deadline + extend` within
   `SINGULAR_RUN_CONTROL_MAX_EXTEND_SEC` (default 4 hours). On cancel, use the existing
   `singular_kill_tree` path and exit semantics. Emit exactly one event per newly honored
   generation per bounded invocation, never per poll. Cross-process high-water persistence is
   outside this task; a replay remains an externally signed authorization bound to the same run.
4. Preserve byte-identical no-control behavior. Use a separate host-selected `control_dir`; it is
   not a secrecy or ACL boundary, and the child may know and overwrite it. Do not
   relocate the legacy capture/output `run_dir`, remove child-visible environment variables, or
   add files when no valid control exists. A child may overwrite control bytes, but without the
   external private key it cannot author an honored extension. The unbounded-timeout path
   (`timeout 0`) stays unchanged.

Recovery boundary: `RUN-20260814T010432Z-76911` and candidate
`refs/singular/operator-candidates/TASK-0115/RUN-20260814T010432Z-76911-attempt-3`
are evidence-only. Do not cherry-pick the path-hiding implementation. Start from the amended
target and capture a new strict RED before any production edit.

Second recovery boundary: `RUN-20260814T022422Z-15249` and candidate
`refs/singular/operator-candidates/TASK-0115/RUN-20260814T022422Z-15249-attempt-2` at
`032a32c018d8d83c1702991fdbda115b94ab8bca` are also evidence-only. Its second auditor accepted,
but final refresh failed the cumulative fresh-input canary at `147124 >= 100000`; more
importantly, neither product attempt satisfied the strict chronology below. Attempt 1's RED
stopped because the helper was missing. Attempt 2 ran already-modified production, passed the
baseline-kill assertion, and failed only an intentional TODO sentinel before adding the final
tests. A fresh reset must capture the behavioral RED from the unmodified amended target before
any production edit. Only after that RED may the candidate be consulted as a reference for a
new implementation. The rerun must additionally fix and prove that a valid signed control has
no effect when Codex timeout is zero, and its no-valid-control golden must compare the frozen
target behavior to the candidate rather than two modes of the candidate.

Third recovery boundary: `RUN-20260814T132932Z-92946`, committed attempt-3 head
`ef255892f972c2512b23430151c5afe3ad7c3c29`, and preserved attempt-4 evidence ref
`refs/singular/operator-candidates/TASK-0115/RUN-20260814T132932Z-92946-attempt-4-uncommitted`
at `03d9e6674f06fdb2d5aaf9859d8578cf7aeabc1b` are evidence-only. Later attempts overwrote the
valid first-attempt behavioral RED with post-production REDs, and the host gate ultimately
failed the valid-extension fixture. The candidate also proved that sandbox `-w` checks do not
establish host trust for user-owned verifier paths. A fresh reset must restore the amended base,
capture the original signed-extension/kill behavioral RED before any production edit, and only
then consult either preserved candidate. It must not reuse a prior RED, audit, gate, or canary.

Fourth recovery boundary: `RUN-20260814T144110Z-64875` and preserved evidence ref
`refs/singular/operator-candidates/TASK-0115/RUN-20260814T144110Z-64875-attempt-2-uncommitted`
at `a2cc125aa78cee08db77ec0428cd16623152cb11` are evidence-only. Attempt 1 did capture the
required exact-base signed-extension RED, but attempt 2 overwrote `.singular-evidence/red.log`
first with PASS and then with a post-production small-order-key failure. The host gate never
reached the nested Codex runner's run-control behavior because that fixture inherited
implementer capability identity locally and failed as role `unknown`/profile `default` on the
host. The evidence seed contains useful bounded-read, isolation, RFC3339, and small-order
hardening, but it has no valid
gate/audit/canary and still lacks the complete golden and adversarial proof below. A fresh reset
must capture and preserve one canonical base RED for the entire run; later attempts write
attempt-specific logs and never overwrite it. Only after that RED may this evidence ref be
inspected, and every retained byte must be revalidated rather than assumed accepted. The
terminal decision log's automatic "same uncommitted changes" rationale compared only the
unchanged Git HEAD and is not byte-equality evidence: attempt 2 did change the test and helper to
add the small-order forgery proof and fix.

Fifth recovery boundary: `RUN-20260814T154054Z-11264` is evidence-only and was deliberately
terminated by the origin before any production/schema edit, gate, or audit. Its canonical RED
(SHA-256 `1101d8017e3994eedbd543346515af585a2db58268b25c2d9edfc3ffb20b95fb`) failed only on
`signed run-control helper is missing`; that is the structural precondition this contract
forbids. An earlier redirect also failed because `.singular-evidence/` did not exist. Attempt 1
was archived as interrupted `worker-no-packet`, attempt 2 made no edit, and neither attempt read
`a2cc125aa78cee08db77ec0428cd16623152cb11`. A fresh reset is mandatory. The canonical RED path
must be absent at dispatch, its parent directory must exist before redirection, and structural or
setup failure terminalizes the run without an in-run retry. Only a child-executed, pre-expiry
signed-extension fixture whose legacy timeout fires and whose failing assertion is exactly the
extension-not-honored behavior may become the canonical RED. Hash it immediately, never replace
it, then inspect the seed, verify its exact tree
`dc0f59ebf82ae8d45c910335a980f429664d0d24`, and revalidate every retained byte.

Sixth recovery boundary: `RUN-20260814T155328Z-24392` is evidence-only and was deliberately
terminated by the origin. Its 86-byte canonical RED (SHA-256
`87c1ba7b2ae5e9651eced7c58ab94e20555732dd2dd01802eb466d8934db572a`) failed inside OpenSSL
with `unable to determine file size for oneshot operation` / `Public Key operation error` before
the supervised child ran. The host gate reproduced those same bytes, and attempt 2 was stopped
before work. Only untracked `tests/test-run-control.sh` changed; no production/schema edit,
seed read, audit, canary, paired review, or inbox exists. The next fresh fixture keeps the private
key on its parent-only descriptor but writes the non-secret documented newline `field=value`
payload (including its final newline) to a seekable file, signs it with `pkeyutl -sign -rawin`
using `-in`, and exports the DER public key through
`SINGULAR_RUN_CONTROL_PUBLIC_KEY_DER_B64`. It clears capability-profile JSON, pins
`SINGULAR_CODEX_BIN` to the exact stub, passes `--no-output-capture`, and requires the stub to
write a parent-chosen child-start marker before sleeping. Use a non-racy window (for example,
child 4 seconds, timeout at least 2, extension at least 5) and accept only
`FAIL: valid pre-spawn signed extension was not honored (runner exited 124)` as the canonical
behavioral RED.

Seventh recovery boundary: `RUN-20260814T225616Z-29482` is evidence-only and terminally
invalid. It captured the correct 75-byte behavioral RED, SHA-256
`3d02edebc218d34bc5036ddefca70d64eb00f58555ed9d21ccb5bf15febd1fa0`, against owned-path
baseline bytes from `fb3d5e087adabf371fa5a8aaa4aa202f9181b1b6`. It then edited production/schema
paths after inspecting the seed but before recording the required base/current/seed blob table
and before materializing all 12 owned paths. It produced no packet, gate, or audit. Preserve it
only under
`refs/singular/operator-invalid-runs/TASK-0115/RUN-20260814T225616Z-29482-attempt-1`
(tree `9fd21157813ad8870026ad9d3fb5e2d23a537da5`); none of its implementation bytes are
admissible.

The next execution must use a new run ID and a newly created worktree at the clean recovery
checkpoint recorded in its dispatch evidence. Before RED, the eight tracked owned paths must
remain byte-identical to the `fb3d5e08` baseline; `engine/run-control.sh`, both new schemas,
`tests/test-run-control.sh`, and the canonical RED path must be absent. Before seed-object access
or any production/schema edit, the worker may create only `tests/test-run-control.sh` and
`.singular-evidence/` chronology artifacts. Capture RED with no-clobber semantics; accept only
exit 1 and the exact line
`FAIL: valid pre-spawn signed extension was not honored (runner exited 124)\n`, 75 bytes,
SHA-256 `3d02edebc218d34bc5036ddefca70d64eb00f58555ed9d21ccb5bf15febd1fa0`.
Immediately preserve the RED, fixture source, and hash attestation outside the worker worktree.
Any wrong output, setup, OpenSSL, capability, output-capture, scheduling, child-not-started,
pre-RED seed access, or pre-RED production/schema change terminalizes the run without retry.

Only after that RED lock may seed `a2cc125aa78cee08db77ec0428cd16623152cb11` be opened;
it must resolve to tree `dc0f59ebf82ae8d45c910335a980f429664d0d24`. Before any
seeded byte is changed, emit and preserve a base/current/seed table for all 12 owned paths,
materialize all 12 paths exactly from that seed (even bytes known to need later correction), and
verify every mode and blob. Only the origin may then create
`.singular-evidence/seed-materialization.ok`. No further edit is permitted before that marker.
After the table and exact copy, the worker must stop editing and wait read-only for the marker;
it must not create, replace, or pre-authorize the marker itself. The origin independently checks
the preserved table and all 12 worktree blobs before writing it.
Missing table, partial materialization, mode/blob mismatch, or any edit before the marker
terminalizes the run without retry. The canonical RED, external authority copy, fixture
snapshot, hash attestation, and materialization attestation remain immutable across attempts;
attempt 2+ output must use attempt-specific files.

## Scope

Owned files:

- `engine/run-control.sh`
- `engine/codex-run.sh`
- `engine/claude-run.sh`
- `engine/cursor-run.sh`
- `engine/grok-run.sh`
- `engine/gemini-run.sh`
- `engine/opencode-run.sh`
- `engine/gate-check.sh`
- `engine/audit-verify.sh`
- `schemas/run-control.v1.schema.json`
- `schemas/orchestration/run-control.v1.schema.json`
- `tests/test-run-control.sh`

Forbidden files:

- Any file outside the owned scope. Do not touch `engine/l1-drive.sh` or `engine/lib.sh`.

## Acceptance Criteria

- New focused test `tests/test-run-control.sh` is authored and run RED against the unmodified
  amended target before any production edit: with a short budget and a stub child that outlives
  it, an externally pre-signed, correct-run extension written before expiry is ignored by the
  baseline and the kill still fires. The RED must fail on the same behavioral assertion that
  turns GREEN after implementation—not on a missing helper, structural precondition, or TODO
  marker. Before this assertion fires the test must not require, source, or assert the existence
  of `engine/run-control.sh` or either new schema; it must prove the intended baseline child/gate
  actually executed. A redirect/setup/child-not-started failure is invalid and terminalizes the
  run without an in-run retry. GREEN: the exact fixture survives and the honored generation
  appears exactly once in the events log. The canonical `.singular-evidence/red.log` remains
  byte-identical for the entire run; a retry that needs another RED writes an attempt-specific
  log and never replaces the original strict chronology artifact.
- The strict RED signer uses a seekable canonical-payload file and the documented newline
  payload/DER-key environment contract; the private key remains only on the parent-held
  descriptor and is closed before child spawn. The nested Codex fixture clears ambient
  capability configuration, pins the exact stub executable, disables legacy output capture, and
  proves child start before evaluating the timeout result. A key-operation, capability,
  output-capture, or scheduling/setup failure cannot be promoted to RED.
- In a recovery run, after the canonical RED and before any production/schema edit, inspect the
  exact recovery tree named above and materialize all 12 owned paths from it. Record a
  base/current/seed blob table before changing the seeded bytes. Missing seed-read,
  materialization, tree, or blob evidence is terminal for that run rather than permission to
  recreate the implementation.
- Generate a test-only private key only in parent-process memory or an anonymous pipe before the
  child exists, derive the public key and pre-sign every required record, then erase the private
  bytes before spawning the supervised child. Prove no private key bytes/path are ever written,
  exported, passed in argv, or child-visible. Give an adversarial child the exact real
  control path and an attacker key; its create, replace, tamper, wrong-run/key, and replay/lower-
  generation attempts never extend the original deadline.
- Extension applied after the original deadline already fired changes nothing (no
  resurrection); the loop checks the effective deadline before polling while preserving a
  record observed before expiry.
- `cancel: true` terminates the child within one poll interval plus kill grace, via the existing
  kill path, with the loop's existing exit semantics.
- Malformed/duplicate-member/extra-field/unsigned/modified/wrong-run/wrong-key/replayed-or-lower/
  oversized/negative records and semantically impossible RFC3339 timestamps are ignored (fail
  closed); per-invocation generation, monotonic extension, sticky cancel, one-event accounting,
  and the extension cap are pinned. A correctly signed record with `reason: ""` is honored once
  and its event preserves `reason` as the empty string. Symlink, any non-regular file (including
  FIFO/device/socket/directory), and a deterministic change-during-snapshot substitution neither
  block the parent nor become valid records.
- The authoritative and consumer v1 schema files are byte-identical, parse as JSON, declare the
  runtime hard protocol ceiling, and pass `tests/test-schema-sync.sh`; the configured
  per-invocation cap may be lower, while any above-ceiling value is clamped down.
- Before spawn, verification pins a root-owned, non-group/other-writable canonical regular
  executable chain or an equivalently immutable already-resident in-process verifier that never
  executes through a mutable path. A same-UID child replacing PATH entries, a user-owned verifier
  target, or any symlink/parent component cannot cause any control to be honored; valid records
  are intentionally inert when no trusted verifier exists, without changing the no-control path.
  If the verifier loads runtime modules/libraries after spawn, their resolved origins and parent
  chains are pinned by the same rule before spawn. Child-controlled cwd/PATH/PYTHONPATH/module
  shadows do not execute. RFC 8032 known-valid, known-invalid, non-canonical, identity, and weak-
  key vectors match the chosen established or complete strict verifier.
- At least one provider runner test and the gate-check test both prove the re-read; the
  remaining runners share the same sourced helper call (grep-asserted in the test). Nested runner
  fixtures explicitly pin a valid role/capability profile or clear capability configuration; no
  test depends on ambient implementer variables, and the authoritative host gate proves this.
- With no valid control present, all six changed providers and both gate loops retain byte-
  identical child environment, output, exit status, and capture/state files compared against a
  frozen unmodified-target baseline (behavioral golden, not grep-only or candidate-vs-candidate
  evidence). Every golden asserts that the intended child/gate actually executed rather than
  comparing equal setup failures. The Codex golden includes a terminal-success event arriving at
  the original deadline and preserves the frozen completion/grace result while checking expiry
  before only the new control poll.
- Timeout-zero behavior is unchanged even when a valid signed extension or cancel record exists:
  the unbounded Codex path never becomes finite, never honors cancel, and emits no control event.
