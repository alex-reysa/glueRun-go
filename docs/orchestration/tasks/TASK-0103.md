# TASK-0103: experiment-run operator hand-off — strict-test-first toolchain complete; remaining requiredCompletion is operator-driven

Status: integrated
Area: eval
Target branch: `agent/integration`
Worker branch: `agent/eval/TASK-0103-experiment-run-operator-handoff`
Test policy: `strict_test_first`
Gate command: `bash tests/run.sh`
Dispatch mode: canonical
Depends on: []

## Objective

This is a planner hand-off record for the `experiment-run` executable DAG node (layer `evaluation`), not a fabricable strict-test-first worker slice. The node's ENTIRE strict-test-first supporting toolchain is integrated and no further engine/CLI brick remains that is not gratuitous or duplicative:

- Raw-metric aggregators: escape-rate/cost/bias (TASK-0077), resume/rehydrate hit-rate + gate-refusal reason-mix (TASK-0079), attempts-to-accept + findings-per-attempt (TASK-0081).
- Treatment-vs-control arm-delta (TASK-0089); composed summary bundle (TASK-0083).
- Renderers: per-arm tables (TASK-0085), corpus pipeline (TASK-0087), result-section delta+audit (TASK-0098), full report body (TASK-0100).
- Provenance + validity: per-run knob-state emitter (TASK-0093), l1-drive recording hook (TASK-0095), per-arm knob-state consistency audit (TASK-0097).
- Operator CLI surface `gluerun experiment-report` behind GLUERUN_CTX_EXPERIMENT (TASK-0091).

The SOLE remaining requiredCompletion behavior is OPERATOR-DRIVEN (kind: evaluation) and, per `docs/orchestration/areas/eval/state.md` ("The L1 planner must NOT generate L2 tasks for this area") and planner-contract rule 7, must NOT be generated as an L2 worker task, because it does not fit strict-test-first mechanics and only the operator can perform it:

1. Run the A/B experiment on a real backlog (>= 40 accepted tasks per arm where feasible): arm A control = M0 knob-state (continuity knobs OFF), arm B treatment = critique+revision + context packets + routing + rehydrate ON, with GLUERUN_PAIRED_AUDIT_PCT=25 in BOTH arms and GLUERUN_CRITIC_RECHECK_PCT=25 in arm B, via GLUERUN_CTX_AB.
2. Author `docs/context-build-plan/experiment-report.md` merged with the escape-rate, cost, and bias measurements plus the secondary and treatment-effect tables, produced from the integrated tooling above.
3. Record the per-knob default decisions in `docs/orchestration/decisions.md`.
4. Publish `docs/orchestration/gates/experiment-run.gate-result.json` manually with the evidence attached (gate results are the only completion authority for this area).

The planner cannot advance this node further: it is operator-blocked, not planner-advanceable. No engine code, schema, or CLI hook would move the unpublished gate, because the gate is published by the operator after the run. This record exists to surface that hand-off rather than fabricate additional gratuitous measurement bricks (the toolchain is exhaustively built). The planner neither declares nor gates node completion; completion authority remains the manually-published gate result.

## Scope

This node is operator-driven; there are no worker-owned code files. The remaining deliverables (`docs/context-build-plan/experiment-report.md`, `docs/orchestration/decisions.md`, and the manually-published gate result) are operator-authored, not worker-owned.

Owned files:

- Any file outside the owned scope.

Forbidden files:

- Any file outside the owned scope.

## Acceptance Criteria

- This is an operator hand-off record: the operator-driven steps in the Objective (run the paired-audited A/B experiment across both arms, author `experiment-report.md`, record per-knob default decisions in `docs/orchestration/decisions.md`, publish the gate result with evidence) are the node's completion path and are not satisfiable by a strict-test-first worker.
- No further strict-test-first engine/CLI brick advances `experiment-run`: its aggregator, delta, summary, renderer, report-body, pipeline, provenance, audit, and CLI surfaces are all integrated, so any additional code task would duplicate integrated work.
- The node remains incomplete until the operator publishes `docs/orchestration/gates/experiment-run.gate-result.json` with the experiment-report and per-knob decisions attached; the planner does not and cannot declare it complete.

## Context packet

### Decisions

- Emit an operator hand-off record rather than another measurement/renderer/CLI brick, because the experiment-run strict-test-first frontier is exhausted (every supporting surface is integrated) and the remaining requiredCompletion is operator-driven, which the area constraint and planner-contract rule 7 forbid the L1 planner from tasking.

### Assumptions

- [validated] `experiment-run` is an operator-driven evaluation node whose gate result is published manually with evidence; the L1 planner must not generate L2 tasks for the eval area — basis: docs/orchestration/areas/eval/state.md and docs/orchestration/planner-contract.md rule 7.
- [validated] The full experiment-run measurement/provenance/audit/renderer/CLI toolchain is integrated (TASK-0077 through TASK-0100, odd ids) — basis: the existing-tasks list and engine/ctx-experiment-*.sh plus cli/gluerun.
- [open] `docs/context-build-plan/experiment-report.md` does not yet exist and the gate is unpublished; the node advances only when the operator runs the experiment and publishes the gate — basis: repository state (no experiment-report.md, no experiment-run.gate-result.json).
