# TASK-0125: rel-99-release — 0.19.0 release notes, contract docs, and full-suite release gate

Status: ready
Area: eval
Target branch: `agent/integration`
Worker branch: `agent/eval/TASK-0125-release-0-19`
Test policy: `strict_test_first`
Gate command: `bash tests/run.sh && bash tests/field-report-canary.sh`
Dispatch mode: canonical
DAG node: rel-99-release
Depends on: [TASK-0116, TASK-0117, TASK-0118, TASK-0121, TASK-0122, TASK-0123, TASK-0124]

## Objective

Close the reliability program with the release artifacts and the whole-suite proof. This node
carries `authority: operator` in the DAG — an agent may prepare everything, but promotion is
an operator decision.

Required changes:

1. `CHANGELOG.md`: a 0.19.0 section in the house style (defect-first narrative, one entry per
   shipped rel-node, each naming the field failure it closes and its rollback knob):
   per-try worker artifacts; try hygiene; integrity-violation reclass; evidence-only remedy
   (`SINGULAR_EVIDENCE_REMEDY_MAX`); bounded manifest remedies (`SINGULAR_EVIDENCE_INFRA_MAX`);
   durable terminal handoff; read-only run-control (`SINGULAR_RUN_CONTROL_MAX_EXTEND_SEC`);
   ctx-route reviewer transcript fix; L1 lease liveness (`SINGULAR_L1_STALE_HARD_MINUTES`);
   failureDomain + attempts-index schema; supervised jobs (`engine/supervised_job.py`,
   `job.v0`); shared identity canonicalizer; gate attach; run-status v1; snapshot checkpoints
   (`checkpoint.v0`); infra-try resume (`SINGULAR_INFRA_TRY_RESUME`, default OFF).
2. `README.md`: update the affected contract sections — Context continuity (per-try artifact
   contract), the operator env-knob table (every new knob above with defaults), Gates and
   audits (supervised execution + attach-by-identity, evidence binding unchanged), recovery
   (L1 liveness, hard-cap deferral), and a new short "Checkpoints are not acceptance"
   paragraph stating the snapshot-grade containment rule.
3. Bump every release-version surface to 0.19.0: `VERSION`, `.singular-version`,
   `singular.config.json` `engineVersion`, and `templates/singular.config.json`
   `engineVersion`. `SCHEMA_VERSION` and both config `schemaVersion` values stay `v2` — every
   new record this sprint is additive (`job.v0`, `checkpoint.v0`, `run-control.v1`,
   `run-status.v1`, `attempts-index.v0`); assert in the changelog migration note that nothing
   is required of existing consumers, matching the 0.17.0 precedent.
4. Verify the release gates hold before marking ready-for-promotion: full `bash tests/run.sh`
   green; `tests/field-report-canary.sh` and `tests/test-fresh-consumer.sh` green (they run
   inside the full suite — if either is currently excluded from `tests/run.sh`, run and cite
   them explicitly in the packet evidence).
5. Refresh only the two content-address pins in
   `tests/fixtures/field-report/spokit-localization-26.capture.v0.json` for the current
   captured fixture bytes: `dag.sha256` becomes
   `23c3992427ef8330af55025bbfc95676ca450f774a34a3cee3acfcba57adf318` and
   `events.sha256` becomes
   `48a7db9baa07a286276de31abd5bf00babfcb8c545dd5730b6aff60ecb748e2d`.
6. Treat paired audit as sampled observability, not hidden acceptance authority. Enumerate every
   paired-audit artifact produced by this sprint, its primary/paired verdicts, disagreements,
   findings, and the durable operator disposition. The rel-99 human gate must refuse approval
   if any non-accepted verdict, disagreement, or finding lacks explicit operator disposition,
   and must always refuse if any packet rejected by a paired audit was imported or integrated.
   This closes FR-049 at the operator boundary without pretending the current post-accept hook
   is fail-closed.

## Scope

Owned files:

- `CHANGELOG.md`
- `README.md`
- `VERSION`
- `.singular-version`
- `singular.config.json`
- `templates/singular.config.json`
- `tests/fixtures/field-report/spokit-localization-26.capture.v0.json`

Forbidden files:

- Any file outside the owned scope. In the owned field-report capture, only the two named hash
  pins may change; no referenced fixture bytes or any other capture field may change. No engine,
  schema, or test changes in this task — a red suite or canary is a blocker to escalate, never
  something this task fixes in place.

## Acceptance Criteria

- CHANGELOG 0.19.0 section lists every shipped rel-node with its knob and rollback, and a
  migration note ("nothing is required; schemaVersion stays v2").
- README knob table documents every new `SINGULAR_*` knob with its default; the checkpoint
  containment paragraph exists.
- `VERSION`, `.singular-version`, and both config `engineVersion` values read 0.19.0;
  `SCHEMA_VERSION` and both config `schemaVersion` values remain v2.
- The capture diff changes exactly `dag.sha256` and `events.sha256` to the required values;
  every referenced fixture and every other capture field remains byte-identical.
- `bash tests/run.sh` and `bash tests/field-report-canary.sh` pass in the task worktree, with
  `tests/test-fresh-consumer.sh` green in the full-suite evidence.
- Release evidence contains the complete paired-audit/disposition inventory; every sampled
  rejection, disagreement, or finding is resolved explicitly, and no paired-rejected packet
  appears in imported history or integration ancestry.
