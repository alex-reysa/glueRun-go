# TASK-0072: context-graph.v0 mapping-table S0–S5 coverage completion (graph-contract)

Status: integrated
Area: graph
Target branch: `agent/integration`
Worker branch: `agent/graph/TASK-0072-mapping-s0-s5-coverage-completion`
Test policy: `strict_test_first`
Gate command: `bash tests/run.sh`
Dispatch mode: canonical
Depends on: []

## Objective

Complete executable DAG node `graph-contract` (stage `S6-graph`, layer `contract`, single-slice) against its `requiredCompletion` clause **event-to-graph mapping table covers all S0-S5 record types**. The integrated contract slice shipped `schemas/context-graph.v0.schema.json`, the fixtures, `docs/context-build-plan/graph-event-mapping.md`, and `tests/test-context-graph-schema.sh` — but the mapping table and its coverage assertion enumerate only a SUBSET of the durable S0–S5 record/event vocabulary actually emitted by the integrated engine. Several durable S0–S5 event types are silently dropped (named nowhere in the mapping doc; grep count 0), which the prior worker's own `[open]` assumption explicitly warned against.

Confirmed-missing durable S0–S5 event strings (emitted via the record/event helpers, absent from the mapping doc):

- `context.resume_failed` — S5 routing resume-refused decision (`engine/ctx-plan-revise-resume.sh`, `engine/ctx-critic-recheck-run.sh`, `engine/generate-tasks.sh`); a sibling of the already-mapped `context.strategy_selected`.
- `plan.revise_parked` — S3 bounded-revise park outcome (`engine/ctx-plan-revise-loop.sh`); a sibling of the already-mapped `plan.revised`.
- `decision.recorded` — the canonical durable decision-record event (`engine/record-decision.sh`, `engine/lib.sh`); the doc references a generic decision record in prose but never the emitted string.
- `ctx.packet_malformed` (S4 context-packet parse failure), `ctx.plan_critique_infra`, `ctx.plan_critique_retry` (S2 critic infra/retry) — telemetry/error events that are still S0–S5 sources and must be accounted for rather than dropped.

This is a CONTRACT-completeness slice on the existing `graph-contract` deliverables only: no schema change (node/edge taxonomy is already complete and green), no fixtures change, no projector, no CLI, no engine wiring, no feature flag. It extends the mapping table's coverage and the test's coverage assertion so the table demonstrably accounts for every S0–S5 record/event type.

One cohesive slice, strict-test-first, in implementation order:

1. Extend the `S0_S5_RECORDS` coverage array in `tests/test-context-graph-schema.sh` to additionally require each confirmed-missing durable S0–S5 string above is named in the mapping doc (RED first: the assertion fails because the doc does not yet name them; keep the assertion POSITIVE — `grep -qF` each string present in the doc, per planner-contract no-negative-assertion rule). Do not weaken or remove any existing assertion.
2. Extend `docs/context-build-plan/graph-event-mapping.md` so every newly-required string is mapped to its node and/or edge projection (or, for pure telemetry/error events, an EXPLICIT `telemetry — not projected` disposition), placed under the correct Stage 0–5 section, preserving the authoritative/claim invariant. All newly-covered events are model- or host-decision or telemetry records → `claim`; NONE may be minted `authoritative` (that family stays reserved for host-verified `commit` / `gate-result` / `secret-scan` / `scope-check`). GREEN after.

Objective names the executable DAG node `graph-contract` and layer `contract` as required.

## Scope

Owned files:

- `docs/context-build-plan/graph-event-mapping.md`
- `tests/test-context-graph-schema.sh`

Forbidden files:

- `schemas/context-graph.v0.schema.json`
- `tests/fixtures/context-graph/nodes.jsonl`
- `tests/fixtures/context-graph/edges.jsonl`
- Any file outside the owned scope.

## Acceptance Criteria

- `bash tests/run.sh` passes green (full suite; this task owns pre-existing files, so the scoped-gate exemption does not apply).
- Strict-test-first is honored: the extended `S0_S5_RECORDS` assertion in `tests/test-context-graph-schema.sh` fails closed (RED) when the mapping doc does not yet name the added strings, and passes (GREEN) once the doc names them. No pre-existing assertion is removed or weakened.
- `docs/context-build-plan/graph-event-mapping.md` names, under its correct Stage 0–5 section, each of `context.resume_failed`, `plan.revise_parked`, `decision.recorded`, `ctx.packet_malformed`, `ctx.plan_critique_infra`, and `ctx.plan_critique_retry`, each with its node/edge projection OR an explicit `telemetry — not projected` disposition; `tests/test-context-graph-schema.sh` asserts each of these strings is present in the doc (`grep -qF`).
- The authoritative/claim invariant is preserved: every newly-mapped event resolves to `claim`; the doc introduces no path by which a model-authored or telemetry record becomes `authoritative`. The existing evidence-class assertions (`evidenceClass` enum exactly `authoritative|claim`, authoritative reserved for host-verified records) remain green.
- No change to `schemas/context-graph.v0.schema.json` or the fixtures; the node/edge taxonomy-completeness and fixture-validation assertions remain green. No engine/CLI wiring and no feature flag are introduced.
- Gate candidate for `graph-contract`: the schema validates the fixture corpus, the mapping table now covers ALL emitted S0–S5 record/event types, and the full suite is green. (Candidate only; completion authority is the `graph-contract` node gate result, not this task.)

## Context packet

Decisions:

- Owned files are the two EXISTING `graph-contract` deliverables (the mapping doc and its test), not new files, because the defect is completeness of an already-integrated contract — so the full-suite gate `bash tests/run.sh` applies per the planner-contract existing-file rule, and no new `engine/ctx-*.sh` is warranted.
- Implementation order is test-then-doc: add the new required strings to the test's `S0_S5_RECORDS` array first (RED — `grep -qF` fails against the current doc), then extend the mapping table (GREEN). This keeps the slice genuinely strict-test-first even though the file predates this task.
- Coverage assertions stay POSITIVE (string present in the doc); do NOT add an unwired/absent grep against `engine`/`cli` — the existing test already avoids that (planner-contract rule 9), and `graph-projector` will legitimately reference the schema later.

Assumptions:

- `[validated] the added strings are durable S0–S5 events emitted by integrated engine modules and absent from the mapping doc — grep: context.resume_failed (ctx-plan-revise-resume.sh, ctx-critic-recheck-run.sh, generate-tasks.sh), plan.revise_parked (ctx-plan-revise-loop.sh), decision.recorded (record-decision.sh, lib.sh), ctx.packet_malformed (ctx-packet.sh, ctx-assumptions*.sh), ctx.plan_critique_infra/ctx.plan_critique_retry (ctx-plan-critic.sh, ctx-plan-recritic-run.sh); each has grep count 0 in graph-event-mapping.md`.
- `[open] the telemetry/error events (ctx.packet_malformed, ctx.plan_critique_infra, ctx.plan_critique_retry) may project to no graph node — they must still be ACCOUNTED for with an explicit telemetry-not-projected disposition, never silently dropped (the prior worker's own dropped-type warning)`.
- `[validated] the node/edge taxonomy and fixtures are already complete and green — this slice must not touch the schema or fixtures, only mapping coverage and the test's coverage assertion`.

Rejected alternatives:

- Re-authoring `schemas/context-graph.v0.schema.json` or the fixtures — rejected: the taxonomy-completeness and fixture-validation assertions already pass; the gap is purely mapping-table coverage of emitted event strings.
- Adding the coverage into `docs/context-build-plan/stage-6-graph.md` — rejected: the mapping table lives in the dedicated `graph-event-mapping.md`; extend that file, not the stage file (owning the stage file would widen scope and risk doc contention).
- Emitting a fresh full re-authoring of the contract slice — rejected: that would duplicate the already-integrated TASK-0071 slice; this task only closes the residual S0–S5 coverage gap.

## Notes

Respects the planner-contract binding rules: additive documentation + additive test assertions under the graph-contract node's own deliverables, no `engine/lib.sh` ownership, no driver-hook edits, no feature flag (nothing is wired ON). Evidence invariance is preserved — the authoritative/claim split is unchanged and no path is created for a model-authored or telemetry record to become `authoritative`. Advocate/skeptic line respected: mapping table and its guard only.
