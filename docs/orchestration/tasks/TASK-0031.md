# TASK-0031: critic-carryover post-acceptance read-only critic-recheck runner composing sampling + resume authority + disposition recorder (engine/ctx-critic-recheck-run.sh)

Status: ready
Area: plancritic
Target branch: `agent/integration`
Worker branch: `agent/plancritic/TASK-0031-critic-recheck-run`
Test policy: `strict_test_first`
Gate command: `bash tests/test-ctx-critic-recheck-run.sh && bash tests/test-engine-clean.sh`
Dispatch mode: canonical
Depends on: []

## Objective

Advance the executable DAG node `critic-carryover` (stage `S3-plan-revision`, area `plancritic`, layer `engine_runtime`, kind `runtime`) with the brick that finally EXECUTES the post-acceptance recheck: the resume-capable read-only critic-recheck RUNNER that resumes the plan critic over the accepted diff and records the per-finding dispositions. It is the post-acceptance sibling of the integrated re-critique runner `gluerun_plan_recritic_run` (TASK-0026, `engine/ctx-plan-recritic-run.sh`): TASK-0026 re-critiques REVISED candidates in-loop; this runner rechecks an ACCEPTED task after acceptance. It composes ONLY already-integrated functions — the sampling gate `gluerun_ctx_critic_recheck_should_sample` (TASK-0027), the resume authority `gluerun_ctx_critic_recheck_resume_decide` + strategy recorder `gluerun_ctx_critic_recheck_record_strategy` (TASK-0029), and the classifier/recorder `gluerun_ctx_critic_recheck_record` (TASK-0028) — plus the shared runner/session primitives, so it advances the stage rather than duplicating any slice.

Wiring TASK-0030's assembled accepted-diff/prior-findings context (`engine/ctx-critic-recheck-context.sh`, currently `accepted` but NOT yet integrated) into this runner's prompt — enriching both paths, especially the fresh fallback where a fresh critic lacks the in-session prior findings — is a sanctioned follow-up slice and is OUT OF SCOPE here; this brick therefore does NOT depend on TASK-0030. The `l1-drive.sh` post-acceptance hook invoking this runner is likewise a separate follow-up slice and is OUT OF SCOPE here.

Add a new `engine/ctx-critic-recheck-run.sh`, auto-sourced by the existing ctx-loader glob in `lib.sh` (the `engine/ctx-*.sh` convention — this task does NOT own or edit `lib.sh`). It defines NEW functions only; NO existing engine path invokes them, so with the file present-but-uncalled the engine is byte-identical to prior behavior (mirroring TASK-0026). To keep the integrated helpers present-but-uncalled under their own invariance greps, reach them by an assembled prefix (mirroring `engine/ctx-plan-recritic-run.sh`), never by their contiguous literal name, and assemble the `plan-critic.md` prompt filename from parts (S2 contract gate).

Public entry point:

- `gluerun_ctx_critic_recheck_run <node> <run_id> <task_id> <run_dir> <prior_critique_record> [worktree]` — perform at most ONE post-acceptance recheck of the accepted task and return 0 on EVERY path (never blocks or mutates the acceptance). (1) SAMPLING GATE: key on the accepted task (e.g. run_id:task_id) and consult `gluerun_ctx_critic_recheck_should_sample`; when NOT sampled (the default-OFF `GLUERUN_CRITIC_RECHECK_PCT`), return immediately with NO runner, NO event, NO state write (byte-identical). (2) When sampled: resolve the canonical per-node plan-critic session-meta, take the accepted-diff lineage head as the current worktree HEAD, consult the TASK-0029 decider (its single-line `resume <sessionId>` / `fresh <reason>` verdict trusted verbatim; any decide-error degrades to fresh, NEVER upgraded to resume) and record the strategy via `gluerun_ctx_critic_recheck_record_strategy`. (3) Run the plan critic READ-ONLY (--level readonly, -C worktree) on the DEFAULT runner over the accepted diff, writing the recheck self-report to `<run_dir>`: on `resume <sid>` WITH --resume-session, on `fresh <reason>` FRESH (no --resume-session); on an rc-86 resume refusal record a role=plan-critic fresh-fallback event and re-run FRESH; on timeout/infra/absent output fall back to FRESH (fail-OPEN). (4) Call `gluerun_ctx_critic_recheck_record` to classify each prior finding addressed | survives | obsolete against `<prior_critique_record>` and emit EXACTLY ONE `ctx.critic_recheck` event — even a no-output recheck records every prior finding via the conservative `survives` default (recorded, never dropped).

Advocate/skeptic line + evidence invariance: the runner runs the critic READ-ONLY on the DEFAULT runner (cross-provider independence), resumes ONLY through the TASK-0029 skeptic role gate (a plan-critic-role session, NEVER a planner/implementer one), never upgrades a `fresh` decision to resume, weakens no gate, changes no accept/reject outcome, touches no packet/lease/task file/inbox placement, and never makes the un-bypassable fresh implementation auditor bypassable. It fails OPEN so an infrastructure failure never blocks acceptance. Events land only in the pinned `GLUERUN_EVENTS_FILE`.

## Scope

Owned files:

- `engine/ctx-critic-recheck-run.sh`
- `tests/test-ctx-critic-recheck-run.sh`

Forbidden files:

- `engine/lib.sh`
- `engine/l1-drive.sh`
- `engine/ctx-critic-recheck.sh`
- `engine/ctx-critic-recheck-classify.sh`
- `engine/ctx-critic-recheck-resume.sh`
- Any file outside the owned scope.

## Acceptance Criteria

- `engine/ctx-critic-recheck-run.sh` is auto-sourced by the ctx-loader glob in `lib.sh` and defines NEW functions only; no existing engine path calls them, so the engine is byte-identical to prior behavior when the file is present-but-uncalled (advances node `critic-carryover`, layer `engine_runtime`).
- OFF-by-default: with `GLUERUN_CRITIC_RECHECK_PCT` unset, `0`, or non-numeric, the runner is a no-op for every accepted task — the TASK-0027 sampling gate returns not-sampled, so no runner is invoked, no event is appended, and no state is written (byte-identical).
- Sampled resume/fresh paths: when sampled, the runner consults the TASK-0029 resume decider over the canonical per-node plan-critic session-meta with the accepted-diff lineage head, records the strategy via `gluerun_ctx_critic_recheck_record_strategy`, and runs the plan critic READ-ONLY (--level readonly) on the DEFAULT runner over the accepted worktree WITH --resume-session on `resume <sessionId>` and FRESH (no --resume-session) on `fresh <reason>`.
- rc-86 fresh fallback and fail-open: a refused resume (rc-86) records a role=plan-critic fresh-fallback event and re-runs the critic FRESH; a runner timeout, infra failure, or absent output likewise falls back to FRESH; the runner returns 0 on every path and never blocks acceptance.
- Disposition recording: after the recheck the runner calls `gluerun_ctx_critic_recheck_record` to classify each prior finding addressed | survives | obsolete against `<prior_critique_record>` and emit EXACTLY ONE `ctx.critic_recheck` event; even when the recheck produced no parseable output, every prior finding is recorded via the conservative `survives` default (never dropped).
- Skeptic role gate: the runner resumes ONLY through the TASK-0029 decider, which resumes ONLY a plan-critic-role session; it never resumes a planner/implementer session and never upgrades a `fresh` decision to resume.
- Evidence invariance: the runner is read-only and returns 0 on every path; it changes no accept/reject outcome, promotes/quarantines nothing, touches no packet/lease/task file/inbox placement, weakens no gate, and never makes the fresh implementation auditor bypassable.
- Engine cleanliness: the base plan-critic prompt filename is assembled from parts (never a contiguous `plan-critic.md` literal) and the integrated helpers are reached by an assembled prefix, so `tests/test-engine-clean.sh`, the S2 contract gate, and the present-but-uncalled invariance greps stay green.
- `tests/test-ctx-critic-recheck-run.sh` is the strict-test-first proof (red before implementation, green after) with stub runners covering: OFF-by-default no-op; the resume path (WITH --resume-session); the fresh path; the rc-86 fresh fallback; exactly one `ctx.critic_recheck` event carrying the per-finding dispositions alongside the recorded strategy event(s); and no outcome mutation / no extra state writes.
- The gate command `bash tests/test-ctx-critic-recheck-run.sh && bash tests/test-engine-clean.sh` passes (no project-specific symbols introduced in `engine/`).
