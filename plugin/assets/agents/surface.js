/* agents/surface.js — placeholder wired in C1 so main.js composes cleanly; the
   real Agents surface (role grid, drilldown, read-only settings) lands in C3. */
export function initAgents() {}
export function setAgentsActive() {}
export function agentsRoute() {}
export function agentsTick() {}
