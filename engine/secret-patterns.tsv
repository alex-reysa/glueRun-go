# Shared high-confidence credential patterns: <label>\t<ERE>
#
# One definition, five consumers: engine/secret-scan.sh (commit/push gate),
# engine/ctx-artifact-scan.sh and engine/ctx-artifact-quarantine.sh (durable
# context artifacts), engine/l1-drive.sh (worker finalize), and the console
# server's browser-facing redactor (plugin/scripts/singular_graph_server.py).
# Previously this lived as a bash function that l1-drive.sh had to sed/eval out
# of a self-executing script; a data file is readable from both languages.
#
# Rules for adding a pattern:
#   * High confidence only. This set gates commits AND redacts operator logs;
#     a false positive silently mangles evidence the operator needs to read.
#   * Anchor on a vendor prefix or structural marker, never on entropy alone.
#     Bare hex/base64 rules would match git SHAs, which appear on nearly every
#     line of this system's output.
#   * Labels are operator-facing and asserted by tests
#     (tests/test-ctx-artifact-scan.sh, tests/test-ctx-artifact-quarantine.sh)
#     and slugified into the console's [redacted:<slug>] token. Changing a
#     label changes both.
#   * ERE syntax (grep -E). Kept compatible with python re.
Supabase token (sbp_)	sbp_[A-Za-z0-9]{20,}
AWS access key id	AKIA[0-9A-Z]{16}
private key block	-----BEGIN [A-Z ]*PRIVATE KEY-----
JWT / bearer token	eyJ[A-Za-z0-9_-]{8,}\.[A-Za-z0-9_-]{8,}\.[A-Za-z0-9_-]{8,}
GitHub token	gh[pousr]_[A-Za-z0-9]{20,}
OpenAI key	sk-[A-Za-z0-9]{20,}
